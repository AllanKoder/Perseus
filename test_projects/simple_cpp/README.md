Test C++ project for Perseus

This small project contains a sample C++ file with an `@pdoc` block inside
a C-style block comment. It's used to validate that Perseus strips leading
`*` characters and parses YAML from commented source files.
